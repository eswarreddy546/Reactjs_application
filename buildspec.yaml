version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      # Install npm
      - npm install

  build:
    commands:
      # Run build script
      - npm run build

artifacts:
  base-directory: build_practice
  files:
    - '**/*'
